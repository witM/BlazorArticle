@namespace BlazorArticle.Components.ArticleComponents
@using System.Runtime.InteropServices


 @if (Runtime.IsWebAssembly)
    {
        <_AlertCounter Counter="Counter" />
    }
    else
    {
        <_AlertCounter @rendermode="InteractiveServer" Counter="Counter" />
    }


@code {
    [Parameter]
    public int Counter { get; set; } = 0;


  
}
